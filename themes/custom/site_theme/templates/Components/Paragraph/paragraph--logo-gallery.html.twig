<div class="container">
  <div class="row align-items-center pt-5 row-cols-sm-6 g-2 ">
  <!-- Logo Gallery -->
    {% for logo in logo_gallery %}
    <div class="col p-3 gx-3 gy-3">
      <img src="{{logo.details.uri}}" class="card-img-top logo-gallery" alt="{{logo.details.alt}}">
    </div>
      {% endfor %}
  <!-- Logo Gallery -->
  </div>
</div>
